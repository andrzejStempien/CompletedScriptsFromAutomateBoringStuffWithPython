def collatz(number)
    if number % 2 == 0
        print(number // 2)
        return number // 2
    else number % 2 != 0
        print(3 * number + 1)
        return (3 * number + 1)
    break;

input
